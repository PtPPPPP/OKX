# Codex 自动循环总任务

继续推进 OKX 项目的当前开发阶段。

## 工作方式

- 以 `docs/phase-3c-first-commit-safety-audit.md` 为当前基线来源。
- 以本文件的强制安全约束为不可突破的安全边界。
- 每一轮只完成一个最小、明确、可验证的任务。
- 不得跳过当前阶段的验收条件。
- 不得因为局部测试通过就宣称整个阶段完成。
- 不得删除或覆盖无法确认来源的历史修改。
- 遇到遗留未提交修改时，必须先识别并保留。

## 强制安全约束

除非任务文件中存在本轮明确授权，否则：

- 不启动真实交易。
- 不启动 Demo 或 bounded_demo。
- 不调用 OKX Broker 写接口。
- 不发送订单、撤单或改单请求。
- 不修改或迁移正式数据库。
- 不访问外部网络。
- 不读取或输出密钥、令牌和账户凭据。
- 不使用 dangerously-bypass-approvals-and-sandbox 或 --yolo。
- 不降低现有安全门禁。
- 不为了让测试通过而删除关键测试或放宽断言。

## 每轮完成条件

本轮必须至少满足：

1. 完成一个明确任务；
2. 运行针对性测试；
3. 检查 Git diff；
4. 更新与本轮任务直接相关的已跟踪文档；
5. 给出准确的完成状态和下一步；
6. 然后结束本轮。

若当前阶段已经全部完成：

- 运行阶段要求的完整验证；
- 更新审计文件；
- 明确写出完成证据；
- 不自动进入下一阶段，除非用户明确授权。
