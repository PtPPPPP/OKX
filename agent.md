# AGENT.md

## 1. 项目定位

本项目用于搭建基于 OKX API 的自动交易与量化研究系统。

系统目标是让同一套策略代码可以运行在以下三种模式中：

1. `backtest`：历史数据回测
2. `demo`：OKX 模拟交易
3. `live`：OKX 实盘交易

开发优先级必须遵循：

```text
正确性 > 风险控制 > 可观测性 > 可维护性 > 性能 > 功能数量
```

任何时候都不得为了快速实现功能而绕过风险控制、订单状态核对或异常处理。

---

## 2. 默认工作模式

除非用户明确要求，否则所有功能默认运行在：

```env
TRADING_MODE=demo
ALLOW_LIVE_TRADING=false
```

禁止在以下情况下发送实盘订单：

* 用户没有明确要求启用实盘交易
* `TRADING_MODE` 不是 `live`
* `ALLOW_LIVE_TRADING` 不是 `true`
* API Key 没有通过账户权限校验
* 风控配置不完整
* 账户、持仓或订单状态没有完成同步
* 系统处于异常、降级、数据过期或断线状态
* 自动化测试未通过
* 当前代码仍包含未处理的占位逻辑、模拟数据或 `TODO`

实盘模式必须采用显式双重开关，不能仅通过一个变量启用。

---

## 3. 安全红线

### 3.1 API 密钥

禁止：

* 将 API Key、Secret Key、Passphrase 写入源代码
* 将真实密钥提交到 Git
* 在日志、异常信息、测试快照或前端页面中输出完整密钥
* 使用带提币权限的 API Key
* 在代码中硬编码真实账户信息

必须：

* 使用 `.env` 或安全的密钥管理方式
* 提供 `.env.example`
* 将 `.env` 加入 `.gitignore`
* 建议设置 IP 白名单
* 将模拟盘和实盘密钥完全分离
* 日志中只允许显示脱敏后的密钥标识

示例：

```env
OKX_API_KEY=
OKX_SECRET_KEY=
OKX_PASSPHRASE=

TRADING_MODE=demo
ALLOW_LIVE_TRADING=false
```

### 3.2 交易权限

项目不实现、不调用、不测试任何提币功能。

如果发现 API Key 拥有提币权限，应输出高优先级警告，并拒绝启动实盘交易。

### 3.3 默认拒绝原则

当系统无法确认某个状态时，默认行为必须是：

```text
不下单
```

包括但不限于：

* 无法确认最新价格
* 无法确认账户余额
* 无法确认当前持仓
* 无法确认订单是否已提交
* WebSocket 数据过期
* REST API 连续失败
* 本地状态与 OKX 状态不一致
* 交易规则、数量精度或最小下单量未知

---

## 4. 目标架构

系统按照以下链路设计：

```text
行情数据
  ↓
策略信号
  ↓
风险检查
  ↓
订单计划
  ↓
订单执行
  ↓
订单与成交跟踪
  ↓
持仓及账户同步
  ↓
日志、数据库、监控和告警
```

推荐模块：

```text
app/
├── config/
├── exchange/
├── market/
├── strategies/
├── risk/
├── execution/
├── portfolio/
├── storage/
└── monitoring/

backtest/
tests/
scripts/
data/
logs/
```

---

## 5. 模块职责

### 5.1 `config`

负责：

* 环境变量
* 交易模式
* 交易品种
* K 线周期
* 策略参数
* 风控参数
* 日志参数
* 数据库参数

配置加载后必须完成类型校验和范围校验。

禁止在业务代码中散落读取环境变量。

### 5.2 `exchange`

负责封装 OKX REST API 与 WebSocket API。

包括：

* 鉴权
* 时间戳
* 签名
* 请求重试
* 限频
* 错误码转换
* 行情订阅
* 私有频道订阅
* 账户查询
* 持仓查询
* 下单
* 撤单
* 订单查询
* 交易规则查询

策略模块不得直接调用 OKX SDK 或拼接 OKX API 参数。

所有外部接口返回值必须转换为项目内部模型。

### 5.3 `market`

负责：

* K 线缓存
* 最新成交价
* 盘口数据
* 行情时间戳
* 数据完整性检查
* 数据过期判断
* 历史数据下载
* 实时数据标准化

使用 K 线策略时，默认只在已确认收盘的 K 线上生成信号。

禁止使用尚未收盘的 K 线冒充收盘数据。

### 5.4 `strategies`

策略只负责生成交易意图，不直接下单。

统一输出结构应至少包含：

```python
Signal(
    action="buy | sell | close | hold",
    instrument_id="BTC-USDT",
    timestamp=...,
    reason="...",
    confidence=...,
    metadata={...},
)
```

策略不得：

* 直接读取 API 密钥
* 直接调用交易所
* 自行绕过风险控制
* 直接修改账户余额
* 假定程序启动时为空仓

策略必须能够被独立测试。

### 5.5 `risk`

风控模块拥有最终否决权。

至少检查：

* 最大单笔名义金额
* 最大总仓位
* 最大杠杆
* 最大每日亏损
* 最大回撤
* 最大连续亏损次数
* 最大订单频率
* 重复信号
* 重复订单
* 最小下单量
* 数量精度
* 价格精度
* 可用余额
* 当前持仓方向
* 是否允许加仓
* 是否允许反手
* 止损止盈
* 行情是否过期
* 点差是否异常
* 系统是否处于熔断状态

风控返回结果必须包含：

```python
RiskDecision(
    allowed=False,
    reason="...",
    adjusted_size=...,
    risk_snapshot={...},
)
```

不得只返回简单布尔值而没有拒绝原因。

### 5.6 `execution`

订单执行模块负责：

* 创建订单请求
* 生成客户端订单编号
* 下单
* 撤单
* 查询订单
* 处理部分成交
* 处理拒单
* 处理超时
* 幂等控制
* 订单状态机
* 成交回报
* 本地订单与交易所订单对账

每次下单都必须有唯一的 `client_order_id`。

网络超时时，不得立即重复下单。必须先通过客户端订单编号或订单查询确认原订单是否已经创建。

订单状态至少包括：

```text
created
submitted
accepted
partially_filled
filled
cancel_pending
cancelled
rejected
expired
unknown
```

`unknown` 状态下禁止继续发送同方向重复订单。

### 5.7 `portfolio`

负责维护：

* 余额
* 权益
* 可用保证金
* 当前持仓
* 未完成订单
* 已实现盈亏
* 未实现盈亏
* 每日盈亏
* 交易成本

程序启动后必须先从 OKX 同步真实状态，再允许策略运行。

本地状态不得被视为最终事实。OKX 返回的账户、持仓、订单和成交数据是最终对账来源。

### 5.8 `storage`

初期允许使用 SQLite。

需要持久化：

* 行情数据
* 策略信号
* 风控决策
* 下单请求
* API 响应摘要
* 订单状态变化
* 成交记录
* 持仓快照
* 账户权益快照
* 异常事件
* 策略参数
* 运行实例信息

涉及金额、价格和数量时，优先使用 `Decimal`，不要使用二进制浮点数直接进行关键资金计算。

### 5.9 `monitoring`

必须具备：

* 结构化日志
* 错误日志
* 交易日志
* 心跳
* WebSocket 连接状态
* 行情数据延迟
* API 请求失败次数
* 订单异常告警
* 持仓不一致告警
* 风控熔断告警

日志中必须包含：

* 时间
* 运行模式
* 交易品种
* 策略名称
* 运行实例 ID
* 信号 ID
* 客户端订单编号
* 风控结果
* 订单状态

---

## 6. 回测规范

回测必须尽量模拟真实交易条件。

至少包含：

* 手续费
* 滑点
* 最小下单量
* 数量精度
* 价格精度
* 延迟假设
* 部分成交或成交失败的可配置模型
* 初始资金
* 仓位约束
* 止损止盈
* 资金曲线
* 最大回撤

禁止未来函数，包括：

* 使用当前 K 线收盘价生成信号，又假设以同一收盘价立即成交
* 使用未来 K 线的数据
* 使用事后才能确认的最高价、最低价或指标
* 在全量数据上归一化后再回测
* 使用未来样本优化当前参数

回测报告至少输出：

* 总收益率
* 年化收益率
* 最大回撤
* 夏普比率
* 胜率
* 盈亏比
* 交易次数
* 手续费总额
* 滑点成本
* 最大连续亏损
* 日度或周期收益序列

---

## 7. 模拟盘规范

模拟盘阶段必须验证：

* API 鉴权
* 服务器时间同步
* REST 请求
* WebSocket 公共频道
* WebSocket 私有频道
* 下单
* 撤单
* 部分成交
* 订单状态变化
* 持仓变化
* 账户余额变化
* 断线重连
* 重复消息
* 消息乱序
* 请求超时
* 程序重启后的状态恢复

模拟盘连续稳定运行并通过验收前，不得进入实盘阶段。

---

## 8. 实盘启用条件

只有同时满足以下条件，才允许启用实盘：

1. 用户明确要求启用实盘
2. `TRADING_MODE=live`
3. `ALLOW_LIVE_TRADING=true`
4. 使用独立实盘 API Key
5. API Key 没有提币权限
6. 风控配置完整
7. 账户和持仓同步成功
8. 未完成订单同步成功
9. 自动化测试全部通过
10. 模拟盘验收通过
11. 日志和告警可用
12. 断线重连可用
13. 订单幂等机制可用
14. 每日亏损熔断可用
15. 初始下单金额处于最小测试规模

实盘第一阶段必须采用：

* 单交易品种
* 低频策略
* 小额资金
* 低杠杆或无杠杆
* 严格止损
* 严格每日亏损上限

---

## 9. 建议配置

```env
APP_ENV=development

TRADING_MODE=demo
ALLOW_LIVE_TRADING=false

OKX_API_KEY=
OKX_SECRET_KEY=
OKX_PASSPHRASE=

INSTRUMENT_ID=BTC-USDT
BAR=5m

MAX_ORDER_NOTIONAL_USDT=20
MAX_TOTAL_EXPOSURE_USDT=100
MAX_DAILY_LOSS_USDT=10
MAX_DRAWDOWN_PCT=5
MAX_LEVERAGE=1
MAX_ORDERS_PER_MINUTE=2

STOP_LOSS_PCT=1
TAKE_PROFIT_PCT=2

DATABASE_URL=sqlite:///data/trading.db
LOG_LEVEL=INFO
```

这些值仅作为开发和模拟盘示例，不构成投资建议。

---

## 10. 编码规范

### 10.1 Python

推荐：

* Python 3.11 或更高版本
* 类型注解
* `dataclass` 或 Pydantic 模型
* `asyncio` 处理 WebSocket 和异步请求
* `Decimal` 处理金额、价格和数量
* `pytest` 进行测试
* `ruff` 进行格式和静态检查
* `mypy` 或 Pyright 进行类型检查

禁止：

* 捕获异常后静默忽略
* 使用裸 `except:`
* 在关键交易逻辑中使用全局可变状态
* 在模块导入时自动连接交易所
* 在测试中访问真实实盘账户
* 通过 `sleep` 猜测订单已经成交
* 将 OKX 原始字典传遍整个项目

### 10.2 错误处理

错误需要分层：

```text
ConfigurationError
AuthenticationError
RateLimitError
NetworkError
ExchangeError
MarketDataError
RiskRejected
OrderRejected
OrderStateError
ReconciliationError
CircuitBreakerTriggered
```

所有可重试错误必须采用有限次数、指数退避和抖动。

所有不可确认的订单错误必须进入对账流程，而不是直接重试下单。

---

## 11. 时间与数据精度

必须：

* 使用 UTC 存储时间
* 在展示层再转换本地时区
* 记录交易所时间与本地时间
* 检查本地时钟偏差
* 统一毫秒或纳秒时间戳规范
* 对 K 线、订单和成交消息进行去重
* 对 WebSocket 消息处理乱序和重放

价格、数量和金额必须根据交易品种规则量化。

禁止通过简单的 `round()` 猜测交易所精度。

---

## 12. 测试要求

至少包含以下测试。

### 单元测试

* 配置校验
* 策略信号
* 指标计算
* 仓位计算
* 风控拒绝条件
* 数量精度
* 价格精度
* 止损止盈
* 订单状态机
* 幂等逻辑
* 盈亏计算

### 集成测试

* REST 客户端
* WebSocket 消息解析
* 模拟盘下单
* 撤单
* 订单查询
* 账户同步
* 持仓同步
* 重启恢复
* 数据库持久化

### 故障测试

* 网络超时
* WebSocket 断线
* REST 连续失败
* 重复成交消息
* 消息乱序
* API 限频
* 订单请求超时但实际已成功
* 本地持仓与交易所不一致
* 数据过期
* 数据库写入失败
* 程序异常退出后恢复

---

## 13. Agent 工作流程

编码 Agent 接到任务后，应按以下顺序执行。

### 第一步：理解现状

先检查：

* 项目目录
* README
* 当前配置
* 依赖
* 已有测试
* 交易模式
* 是否存在真实密钥
* 是否可能触发实盘交易

不得在没有阅读现有代码的情况下直接重构核心交易链路。

### 第二步：给出改动计划

对于跨模块任务，先说明：

* 要修改哪些模块
* 为什么修改
* 风险点
* 测试方法
* 是否影响回测、模拟盘或实盘

### 第三步：最小化改动

优先完成可验证的最小闭环。

不要在一个任务中同时大规模修改：

* 策略
* 风控
* 订单执行
* 数据库结构
* 实盘配置

高风险模块应拆分提交。

### 第四步：运行验证

至少执行：

```bash
pytest
ruff check .
```

如果项目配置了类型检查，还需要执行：

```bash
mypy .
```

或：

```bash
pyright
```

如果无法运行某项验证，必须明确说明原因。

### 第五步：输出结果

完成后应汇报：

* 修改内容
* 修改文件
* 风险变化
* 测试结果
* 尚未完成的内容
* 是否具备进入下一阶段的条件

不得声称未实际运行的测试已经通过。

---

## 14. 修改限制

未经用户明确要求，不得：

* 启用实盘
* 增大杠杆
* 增大下单金额
* 关闭止损
* 关闭每日亏损限制
* 删除熔断逻辑
* 删除对账逻辑
* 将市价单作为默认订单类型
* 将模拟盘密钥替换为实盘密钥
* 添加提币功能
* 自动提交 Git
* 自动部署到服务器
* 自动长期运行交易程序

如果用户要求进行上述高风险修改，应明确说明风险，并尽量保留安全开关。

---

## 15. 文档要求

新增功能时同步更新：

* `README.md`
* `.env.example`
* 配置说明
* 启动命令
* 测试命令
* 风险说明
* 模拟盘使用说明
* 实盘启用条件
* 故障恢复说明

任何会改变下单行为的改动，都必须在文档中单独列出。

---

## 16. 第一阶段开发目标

当前默认开发目标是完成一个安全、可测试的最小版本：

```text
Python
+ BTC-USDT 现货
+ 5 分钟 K 线
+ 简单均线策略
+ 历史回测
+ OKX 模拟交易
+ SQLite
+ 固定小额下单
+ 止损止盈
+ 每日亏损熔断
+ 日志
+ 订单和持仓对账
```

第一阶段暂不追求：

* 高频交易
* 多账户
* 多交易所
* 多策略组合
* 跨交易所套利
* 复杂合约策略
* 高杠杆
* 机器学习预测
* 自动参数寻优
* 公网 SaaS 管理平台

---

## 17. 完成定义

一个交易功能只有同时满足以下条件，才算完成：

* 代码实现完成
* 类型和配置校验完成
* 错误处理完成
* 日志完成
* 风控接入完成
* 单元测试完成
* 模拟盘验证完成
* 状态恢复路径完成
* 文档更新完成
* 没有默认启用实盘
* 没有暴露密钥
* 没有已知重复下单风险

---

## 18. 最终原则

始终遵守以下原则：

```text
策略可以失败，但系统不能失控。
网络可以失败，但订单不能重复。
程序可以重启，但状态不能丢失。
数据无法确认时，不交易。
风险检查未通过时，不交易。
实盘未被明确启用时，不交易。
```
